import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "./components/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sonu | Graphic Designer",
  description: "Portfolio of Sonu - A creative graphic designer crafting cute and aesthetic designs that bring joy to everyday life.",
  keywords: ["graphic designer", "portfolio", "cute design", "aesthetic", "illustration", "pastel", "creative"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#fafafa]">
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
